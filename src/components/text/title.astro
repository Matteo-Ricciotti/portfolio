---
import { cn } from "../../utils";

type Props = {
    moreClasses?: string;
};

const { moreClasses } = Astro.props;
---

<h1 class=`${cn("text-4xl font-medium xl:text-6xl", moreClasses)}`>
    <slot />
</h1>
